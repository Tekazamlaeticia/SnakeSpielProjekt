/**The main method creates model, view and controller classes.
 * */

package SnakeGame;
import SnakeGame.Controller.ControllerSnake;
import SnakeGame.View.ViewSnake;

import processing.core.PApplet;

/**
 * the Main creates model, view and controller classes.
 * connects them by calling the setView method and the setController method.
 * Also, the processing application is started by calling the runSketch method.
 */
public class Main {
    /**
     * Main method creates a model, view and controller classes.
     * It connects them and start the processing application.
     */

    public static void main(String[] args){

       /** var view = new ViewSnake();
        var controller = new ControllerSnake(view);
        // Connect controller and view
        controller.setView(view);
        view.setController(controller);
        // Connect controller and view
        PApplet.runSketch(new String[]{"ViewSnake"}, view);*/




    }

}
